#include<iostream>
#include<string>
#include<vector>
using namespace std;

int main(){

    // int n = 7;
    // for(int i = 1; i <= 20; i++)
    //     cout << endl << i << " % " << n << " = " << (i % n);

    // vector<int> vec(10);

    // for(auto x : vec){
    //     if(x[0] != -1 && x[1] != -1)
    //         cout << "Found";
    // }

    // cout << (6 < 7);

    return 0;
}